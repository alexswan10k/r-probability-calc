//import { getProbFn } from "./CalcComp"





describe("Calculator", () => {
    test("it should work", () => {})
    
    // describe("stuff that doesnt work because you need a .wasm loader or mocking (this stuff is tested in rust land anyway)", () => {
    //     it("should call intersect", () => {
    //         expect(getProbFn("intersect")(0.5, 0.5)).toEqual(0.25)
    //     })

    //     it("should call union", () => {
    //         expect(getProbFn("union")(0.5, 0.5)).toEqual(0.75)
    //     })
    // })

    //... you get the idea
})